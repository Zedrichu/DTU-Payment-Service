<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>---->> %d{HH:mm:ss}  %-5level %purple(%logger{36} %method) - %msg%n</pattern>
        </encoder>
<!--        [%thread]-->
    </appender>

    <root level="debug">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>